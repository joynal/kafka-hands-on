import { fakerEN as faker } from "@faker-js/faker";

import producer from "./helpers/producer.js";
import { visitorQueue } from "./config.js";

const run = async () => {
  // TODO: implement sender logic
};

run().catch((e) => console.error(e.message));
